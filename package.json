{
  "name": "strapi-next-app",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "strapi-api",
    "front-next"
  ],
  "scripts": {
    "start:dev": "concurrently \"yarn workspace strapi-api develop\" \"yarn workspace front-next dev\"",
    "start:front:dev": "yarn workspace front-next dev",
    "start:back:dev": "yarn workspace strapi-api develop",
    "start:prod": "concurrently \"yarn workspace strapi-api start\" \"yarn workspace strapi-api start start\"",
    "build": "concurrently \"yarn workspace server build\" \"yarn workspace front-next build\""
},
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
